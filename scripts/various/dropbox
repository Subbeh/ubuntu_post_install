# name: Dropbox

apt_cmd libpango1.0-0 \
        libpangox-1.0-0
TEMP_DEB="$(mktemp --suffix=.deb)" && \
  wget -q -O "$TEMP_DEB" "${DROPBOX_URL:?not set}" && \
  sudo apt install "$TEMP_DEB" >/dev/null
